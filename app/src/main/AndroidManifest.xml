<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required by the app -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Declare camera hardware feature is required -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".CampusApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_campus"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_campus"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusApp"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Splash Activity is the launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CampusApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other App Activities -->
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

        <activity
            android:name=".PermissionsActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

        <activity
            android:name=".IntroActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

        <activity
            android:name=".OrderProductActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

        <activity
            android:name=".WalletActivity"
            android:exported="false"
            android:theme="@style/Theme.CampusApp.NoActionBar" />

    </application>

</manifest>
